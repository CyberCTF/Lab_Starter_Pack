---
description: UI design system: shadcn-like, Tailwind, dark mode, minimalist professional look
globs: ["**/*.tsx", "**/*.jsx", "**/*.html", "**/*.css", "**/*.scss", "**/components/**"]
alwaysApply: true
---

# User Interface Requirements (Dark-Only)

The UI must follow a shadcn/ui-like system with an always-on dark theme and a minimalist, professional look. No light mode, no fancy gradients.

## Design System Requirements

### 1. Component Library
- **Shadcn/ui Components**: Use shadcn/ui component library
- **Consistent Design**: Maintain design consistency across all pages
- **Professional Appearance**: Look like a real company website
- **Minimalist Approach**: Clean, simple, and functional design

### 2. Styling Framework
- **TailwindCSS**: Use TailwindCSS for styling (utility-first only)
- **Dark Mode (Only)**: Enforce dark-only per `ui/DARK_MODE.mdc` (no light toggle)
- **Responsive Design**: Ensure mobile and desktop compatibility
- **Accessibility**: Follow accessibility best practices

### 3. Visual Guidelines
- **No Fancy Colors**: Avoid colorful gradients and flashy elements
- **Professional Palette**: Use business-appropriate color schemes
- **Clean Typography**: Use readable, professional fonts
- **Minimal Animations**: Subtle, purposeful animations only

### 4. Frontend Stack Requirements
- **React + Next.js (TypeScript)**: App Router preferred; SSR/SSG as needed
- **Tailwind**: `darkMode: "class"`; root `html` has `class="dark"`
- **Shadcn/Radix**: Use where suitable; style via Tailwind and tokens
