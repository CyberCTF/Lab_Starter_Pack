---
description: Strict black-only UI for PHP + native HTML/CSS/JS; enforce WCAG AA contrast
globs: ["build/**/src/**/*.php", "build/**/src/**/*.html"]
alwaysApply: false
---

# UI Black-Only Guidelines

## Purpose
- Enforce a strictly black-only UI with grayscale accents across all surfaces and components.
- Provide consistent, accessible, mobile-first UI/UX guidance for the project.

## Non-Negotiable Constraints (Black-Only Theme)
- Color usage must be monochrome: pure black base (#000000) with grayscale only (#111, #222 … #fff). No chromatic hues.
- Backgrounds: black (#000000) by default; surfaces may use very dark gray steps for elevation.
- Text: white (#FFFFFF) on black; use grayscale for secondary/tertiary text ensuring WCAG 2.1 AA contrast.
- States (hover, focus, active, disabled): use opacity, underline, border, shadow, or grayscale shifts—not color.
- Links and CTAs: remain white text; emphasize via weight, underline, size, or container borders. No colored accents.
- Icons/illustrations: monochrome SVGs; tint via white/grayscale only.

## Accessibility
- Follow WCAG guidelines; validate contrast for all text and UI states.
- Use semantic HTML and ARIA where appropriate.
- Provide alt text for images/non-text content.
- Ensure full keyboard navigability and visible focus styles (e.g., 2px white or light-gray focus ring with adequate contrast on black).
- Test with screen readers and high-contrast modes.

## Implementation Notes (Tailwind)
- Only use black/white/gray tokens; no chromatic utilities.
- Prefer opacity, borders, shadows, and spacing for emphasis; avoid color-based emphasis.
- Keep base page background black and primary text white.
- Override any third-party styles to enforce monochrome if needed.

## Enforce Tailwind Black Theme in all labs

### Scope
- Apply to all web pages/templates rendered by the lab (PHP, HTML).
- Update both dev and prod compose health if needed.

### Requirements
- UI must use Tailwind CSS with a strict black/gray palette only (no colors).
- Prefer CDN injection for simplicity; if a build exists, wire Tailwind via PostCSS with the same tokens.
- No inline CSS with colors or gradients. No non‑monochrome styles. Replace with Tailwind classes.
- Body and layout: black background, white foreground, gray surfaces/borders.
- Inputs/buttons/cards use only black/white/gray utilities; focus rings are white/gray.
- Health pages and all HTML templates follow the same theme.

### Implementation (minimum)
- Inject in every web HTML head:

```html
<script src="https://cdn.tailwindcss.com"></script>
<script>
  tailwind.config = {
    theme: {
      extend: {
        colors: {
          black: "#000000", white: "#FFFFFF",
          gray: {50: "#FAFAFA",100: "#F5F5F5",200: "#E5E5E5",300: "#D4D4D4",400: "#A3A3A3",500: "#737373",600: "#525252",700: "#404040",800: "#262626",900: "#171717",950: "#0A0A0A"}
        }
      }
    }
  }
<\/script>
```

- Apply on body:

```html
<body class="min-h-screen bg-black text-white">
```

- Replace colored inline styles with Tailwind classes:
  - Containers: `bg-gray-950 border border-gray-800 rounded`
  - Buttons: `border border-gray-700 bg-black text-white hover:bg-gray-900 focus:outline-none focus:ring-2 focus:ring-white`
  - Inputs: `bg-gray-950 text-white border border-gray-700 placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-white`

### Prohibitions
- No gradients, no colored palettes, no SELECT * in CSS or HTML equivalents.
- No inline style attributes that set background/text colors other than black/white/gray.
- Do not import external themes or CSS frameworks that introduce non‑monochrome colors.

### Validation/Acceptance
- Open the home page: background is black, primary text white; no color hex other than black/white/gray in the rendered DOM.
- Health endpoint also uses the theme (if it renders HTML).
- Lint: scan project for `style=\".*(linear-gradient|#[0-9a-fA-F]{6})\"` and block unless the hex is black/white/gray; reject `background:` with gradients.

### Deliverables
- Updated HTML templates with Tailwind CDN and classes.
- If a build system exists: Tailwind config equivalent to the CDN config and compiled CSS linked from pages.

If you want, I can also give you a one‑shot prompt that says “convert the current index.php to Tailwind black now” for use per project.

### Compose/Health (if needed)
- Web must listen on internal 3206. Apache/Nginx configured accordingly.
- Healthcheck prefers a lightweight route (e.g., `/health.php`) not dependent on DB.
- Compose mapping stays `3206:3206`; healthcheck example: `curl -f http://localhost:3206/health.php`.

### Changes To Make (Ordered)
1) Inject Tailwind CDN + config in the page layout(s) and any standalone pages.
2) Add required body classes and remove all colored inline CSS.
3) Refactor components (buttons, inputs, cards) to the specified Tailwind classes.
4) If missing, create `/build/web/src/health.php` returning 200 and minimal HTML with the black theme baseline.
5) Ensure server config listens on 3206 and compose healthchecks point to the health endpoint.

### Acceptance Criteria (Blocking)
- The DOM of every page contains the Tailwind CDN script and `<body>` has `bg-black text-white`.
- No `linear-gradient` anywhere in the project.
- No color hex outside `#000000`/`#FFFFFF` or gray scale set (gray-50..950) in HTML/CSS.
- Buttons/inputs/cards use the specified classes.
- Web container becomes healthy with curl healthcheck.

### Self-Validation (Must Run)
- Fail the task if any of these regexes match:
  - `style="[^"]*(linear-gradient)[^"]*"`
  - `style="[^"]*#(?!000000|ffffff)[0-9a-fA-F]{6}[^"]*"` (case-insensitive; allow only black/white if inline exists)
- Fail if Tailwind CDN script is missing.
- Fail if `<body>` lacks `bg-black` and `text-white`.
- For PHP/HTML files, search any class or style that implies non-monochrome colors; replace with black/white/gray utilities.

## Tailwind Black Theme Template

Use this template to enforce a black-only (monochrome) design with Tailwind in PHP/native HTML projects.

### tailwind.config.js

```javascript
/** Strict black-only Tailwind theme (PHP/HTML/CSS/JS projects) */
module.exports = {
  content: [
    "./**/*.php",
    "./**/*.html",
    "./**/*.js"
  ],
  darkMode: "class", // optional; default UI is already black
  theme: {
    colors: {
      transparent: "transparent",
      current: "currentColor",
      black: "#000000",
      white: "#FFFFFF",
      gray: {
        50: "#FAFAFA",
        100: "#F5F5F5",
        200: "#E5E5E5",
        300: "#D4D4D4",
        400: "#A3A3A3",
        500: "#737373",
        600: "#525252",
        700: "#404040",
        800: "#262626",
        900: "#171717",
        950: "#0A0A0A"
      }
    },
    extend: {}
  },
  plugins: []
};
```

### styles.css

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  html, body {
    background-color: #000000;
    color: #FFFFFF;
  }
  a { color: #FFFFFF; text-decoration: underline; }
}

@layer components {
  .btn-primary {
    @apply inline-flex items-center justify-center px-4 py-2 rounded border border-gray-700 bg-black text-white hover:bg-gray-900 focus:outline-none focus:ring-2 focus:ring-white;
  }
  .card {
    @apply rounded-lg border border-gray-800 bg-gray-950 p-4;
  }
}
```

### Example HTML (PHP/HTML)

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Black UI</title>
    <link href="/dist/styles.css" rel="stylesheet" />
  </head>
  <body class="min-h-screen bg-black text-white">
    <header class="border-b border-gray-800">
      <div class="mx-auto max-w-4xl p-4 flex items-center justify-between">
        <h1 class="text-xl font-semibold">Application</h1>
        <nav class="space-x-6">
          <a class="hover:opacity-80" href="#">Home</a>
          <a class="hover:opacity-80" href="#">Dashboard</a>
        </nav>
      </div>
    </header>
    <main class="mx-auto max-w-4xl p-4 space-y-6">
      <section class="card">
        <h2 class="text-lg font-medium mb-2">Section</h2>
        <p class="text-gray-300">All content uses black/white/gray only.</p>
        <button class="btn-primary mt-4">Primary Action</button>
      </section>
    </main>
    <footer class="mx-auto max-w-4xl p-4 text-gray-400 border-t border-gray-800">© Company</footer>
  </body>
  </html>
```

### Usage Notes
- Only black, white, and gray tokens are available; no chromatic colors are defined.
- Emphasize states using opacity, borders, shadows, scale, and typography—not color.
- Configure your build to process Tailwind (PostCSS or CLI) and output to `/dist/styles.css` referenced by your pages.